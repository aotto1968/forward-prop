# Makefile — Reference Implementations (OBSOLETE)
# ===============================================
# Adam sources moved to ../mnist/ and ../cifar/.
# Hebbian sources unified in ../mnist/ and ../cifar/.
# Use: make -C ../mnist all  or  make -C ../cifar all
#
# This directory kept for reference only.
# ===============================================

.PHONY: all mnist cifar clean

all:
	@echo "NOTE: Reference sources moved to ../mnist/ and ../cifar/"
	@echo "      Use: make -C ../mnist all   or   make -C ../cifar all"

mnist: all
cifar: all

clean:
	rm -f *.exe
